import {describe, it, expect} from 'jasmine';
import {paramsToQuery} from '../../../src/playground/modules/query.js';

describe('it should convert params to query', () => {
  it('i dont know what im doing', () => {
    expect(true).toBe(true);
  });
});
